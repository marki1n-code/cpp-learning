#include <iostream>

int main () {

    // type conversion = converting data of one data type to another data type
    //      implicit = automatic type conversion
    //      explicit = manual type conversion


    //char  a = 69;

    //std::cout << a << std::endl;  ASCII value (69) converted to char (E)

    std::cout << (char) 69 << std::endl; // explicit conversion from (int) to (char)


    return 0;
}