#include <iostream>
#include <cmath>

int main () {
    double x = 3;
    double y = 4;
    double z;


    //z = std::min(x, y); // Find the minimum of x and y
    //z = std::max(x, y); // Find the maximum of x and y
    //z = pow(2, 4); // Calculate 2 raised to the power of 4
    //z = sqrt(9); // Calculate the square root of x
    //z = abs(-5); // Calculate the absolute value of -5 
    //z = round(x); // Round x to the nearest integer

    std::cout << z;

    return 0;
}